---
title: 计组期中复习文档
date: 2026-04-16 15:16:29
tags:
  - XDU
categories:
  - Exam
---

# 计组

## Ch0 单位换算

1. 1s =1,000 ms  = 1,000,000 us = 1,000,000,000 ns
2. 1KB = 2^10 b
3. 1MB =2 ^20 b

## Ch1 绪论

1. 计算机历史

    - 电子管（ENIC）-晶体管-中小规模集成电路-大规模和超大规模集成电路-巨大规模集成电路
    - 摩尔定律：DRAM每18个月翻一倍，微处理器每24个月翻一倍
    - 冯诺依曼计算机结构首次提出存储程序，是以控制器为中心（现代计算机是以存储器为中心），**没有**将程序指令和数据分别存储在两个独立地址空间存储器中

2. 完整的计算机系统应该包括硬件系统和软件系统

3. 计算机系统的层次结构（课本P17）（注意微程序或硬件逻辑是最底层，且OS在Assembly language下面）

4. ISA是软件和硬件的分界面

5. 计算机的分类

    - Flynn分类
        - 根据指令流（IS）和数据流（DS）分类
        - SISD（最简单），SIMD，MISD，MIMD（多核处理器，多计算机集群，云计算）
        - **冯罗伊曼计算机结构是SISD**

6. 计算机性能描述

    - CPI （Cycles Per Instruction，执行一条指令所需要的平均时间），IPS（Instruction Per Second），MIPS（Million IPS）（见王道）
    - 吞吐量，性能，执行时间

    - Amdahl定律（课本例题）

        注意多核问题问法

7. 图片文件一个像素的位数由色决定，如6.5万色表示有16bit

## Ch2 计算机系统中的数据表示

### 2.1-2.2

1. 原码 vs 补码 vs 反码 vs 移码 相互转换以及表示，多做课后题
2. 注意正数时补码和反码和原码表达方式一致

### 2.3 浮点数

1. 浮点数的表示，常见问法见附件
2. 原码尾数规格化（s.1xxxx） vs 补码尾数规格化（异或）
3. 规格化原码尾数**表示范围**与规格化补码尾数**表示范围**的**数轴表示**
4. 左归与右归
5. IEEE754
    - float vs double：指数e长度 8 vs 11
    - 注意尾数隐含的“1”
    - 尾数用原码表示
    - 注意阶码表示范围，float是-126 -> 127
    - e=0时阶数是-126（以float为例）
    - e=255时f全0表示INF，不全0表示NaN（以float为例）
    - 有+0和-0
    - 舍入模式
        - 就近舍入（距离相等时取偶数）
        - 朝0舍入
        - 朝+INF舍入
        - 朝-INF舍入

### 2.4-2.5

1. ASCII数字0=30H，大写字母A=40H，小写字母a=60H
2. 汉字编码：GB2312-1980（2字节） -> GBK -> GB19030(变长字节1,2,4)

### 2.6 ~~检错与纠错码（疑似取消）~~

1. 奇校验 && 偶校验
2. CRC（循环冗余校验码）
3. 海明码

详细见笔记

## Ch3 运算方法与运算器

### 坑

1. 注意是按字节编址还是按字编址，卷1选择第14题

2. C语言等程序h负数取整是向上取整，如-31/4=-7而不是-8(会加上bias)

    如-31：0xE1；-31/4：0xF9~~而不是0xF8~~

### 3.1 定点数运算

1. 补码加减法及其溢出判断

    - 双符号位（变形码）
    - 进位判决
    - 根据符号位和进位标志（只适用于同号数）
    - 根据运算前后符号位

2. 加法器

    - 行波加法器（串行进位），nt（t是一位全加器的延时）
    - 先行进位加法器（并行进位）t+\delta t（\delta t是并行电路的延时）
    - 上述两者结合的计算时间问题

3. 减法器

    基于加法器实现，掌握电路图

4. 8421 BCD

    - 压缩BCD与非压缩BCD

    - 如果单个BCD码数值超过9 或者 有向更高位的进位则需要加**0110**校正

5. 移码加减法

    - 注意运算结果的符号位要**取反**

    - 判断溢出根据真值或者是否正正得负（负负得正）

6. 原码一位乘法

    - 大题要用Sign-D-A-OP的格式书写，详情见课本
        - 初始化D置零，单符号位
        - 右移是逻辑右移（**空位补0**）
        - 符号位异或得来，下列过程都是绝对值乘法
        - n位小数（包括符号位），计数器循环n-1，D寄存器（不包含符号位），A寄存器都是n-1位
        - 最后一步只需右移就结束
    - 硬件电路框图

7. 补码一位乘法：布斯算法

    - 寄存器级操作
        - 初始化D也是置零
        - 右移是算术右移
        - D寄存器+Sign存储**双符号位**，A寄存器存储**单符号位**即可
        - **A寄存器增加A-1,初始为0**
        - 计数器循环n
        - 符号位参与运算
        - 最后一次循环中可以右移后就结束，但是要注意小数点位置；**也可以不右移，直接把A寄存器的最后一位A0置0这里的小数点位置还是在D寄存器的左侧**
        - 乘积的数值位是乘数的数值位的2倍
    - 硬件电路框图

8. 阵列乘法器（知道是最快即可）

9. 原码除法：加减交替法

    X / Y

    - 被除数的绝对值要小于除数

    - 寄存器级操作
        - 绝对值除法
        - X的数值位的位数应是Y的数值位的位数的两倍，如果不够要填0补充，如X=0.1000，Y=0.11
        - D和A初始化均分X的数值位
        - **OP流程：先置商，然后左移，然后判断是+|Y|还是-|Y|**
        - R >= 0时置商1
        - 与乘法不同，除法是左移，而且开始时肯定不够除，所以一定会先左移一位
        - 初始时D-A寄存器放被除数(X)的值，X采用双符号位，所以Y也要采用双符号位
        - -|Y|用的是|Y|相反数补码求法，也就是说|Y|和-|Y|都用补码表示
        - 结果的余数记得乘2^-4
        - 第一步（OP）：直接左移，不需要置商
        - 最后一步（OP）：置商即可，但是如果最后一步余数为负数的话需要+|Y|恢复余数
        - 余数符号与商符号一致
    - 硬件电路框图

### 3.2 逻辑与移位运算

### 3.3 浮点数运算

1. 浮点数加减运算
    - 浮点数的表示方式：阶码（移码或者补码）；尾数（一般双符号位补码）
    - 步骤：
        - 对阶（阶码小对大，尾数右归时记得看题目要求的舍入处理（常见0舍1入））
        - 求尾数和/差
        - 结果尾数规格化（同上，右归时记得舍入处理）
        - 根据尾数规格化调整阶码
2. 浮点数乘除运算
    - 不同尾数编码左归，右归次数问题

### 3.4 运算器基本结构

1. 单总线 vs 双总线 vs 三总线

    - 单总线至少需要2个暂存器，需要3步操作控制
    - 双总线至少需要1个暂存器，需要2步操作控制
    - 三总线不需要暂存器，需要1步操作控制



## Ch4 存储系统

### 4.1存储系统概述

1. 存储器分类及特点 王道P78

2. 存储器性能指标
    - 存取时间 && 存取周期
    - 存储器带宽

### 4.2主存储器

1. SRAM vs DRAM
    - 地址引脚数，数据引脚数

    - DRAM地址引脚复用 vs SRAM不复用

    - SRAM芯片引脚图

        数据引脚，地址引脚，片选引脚（CS），写引脚（WE），读引脚（OE）以及电源，接地端等引脚。

        注意数据引脚不需要取对数，例如SRAM 1024 x 8 除了电源和接地端最少需要21位引脚

    - DRAM破坏性读出->刷新机制

2. ROM

3. MEMR（Memory Read）存储器读信号

4. MEMW（Memory Write） 存储器写信号

5. LS74138的使能端G，G2A非,G2B非

6. 相联存储器 课本P117

    m个存储单元 = > m个数字比较器，1个m:1 mux

7. 字拓展和位拓展

8. 多体交叉存储器

9. 一些知识

    - 通常用**高位地址线**产生片选信号

### 4.3 Cache

1. 地址映射与变换

    - 全相联
        - **地址映射表**的存储单元个数=Cache块数，每个存储单元的位数=主存块号位数+属性位数（有效位）
        - 主存地址构成：主存块号+块内地址
        - Cache地址构成：块号+块内地址
    - 直接地址映射
        - 无地址映射表
        - 主存地址构成：区号+区内块号+块内地址
        - Cache地址构成：块号+块内地址，**主存地址去掉区号就是Cache地址**
        - 注意主存不分区的第几块如何判断
    - 组相联
        - 主存地址构成：区号（标记，Tag）+组号（索引，Index）+块内地址
        - Cache地址：常问Cache路内地址，也就是Index+块内地址
        - 地址映射表：
            - 有效位+区号（标记，Tag），省略了对应该组内的Cache第几块，
            - 存储单元个数是Cache总块数
        - m路组相联，把Cache分成m路，主存根据每路Cache的大小分区
        - 如何根据主存地址查找Cache块（手写）：先根据Index去地址映射表查找，再根据地址映射表的有效位和比对地址映射表的Tag和主存地址的Tag
        - 组相联无Index位时即为全相联
        - 组相联为1路组相联时即为直接映射
    - **三种地址映射分别的特点以及适用场景**
        - **全相联**需要设置地址映射表，标记存储消耗最多，不适合大容量Cache，速度最慢，命中率最高
        - **直接映射**不需要设置地址映射表，标记存储消耗最少，速度最快，命中率最低
        - 现在计算机采用的大部分都是**组相联**

2. 替换算法

    - FIFO
    - LRU

    都是画栈图，弹栈尾，入栈顶

3. 更新策略

    关注主存与Cache的一致性问题

    - 写回法
        - Cache Hit：将数据写入Cache而不写入主存，Cache被替换出去时才写回主存
        - Cache Miss：将主存块调入Cache，再在Cache中操作
    - 写直达法
        - 在将数据写入Cache的时候同时写入主存，速度慢
    - 特性对比
        - 写回法需要设置标志位，写直达法不需要
        - 多处理器系统通常采用写直达法以保持一致性（Data race）
        - 写回法可以减少Cache与主存的通信量

4. Cache性能衡量

    - 平均访问时间
    - 加速比
    - 多级Cache结构
        - 总缺失率
        - 课本P145 例4.7，4.8

    

    




